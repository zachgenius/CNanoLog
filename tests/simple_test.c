#include <cnanolog.h>
#include <assert.h>

int main() {
    // This is a placeholder test.
    // A real test would capture stdout/stderr and check the output.
    LOG_INFO("Test info message");
    LOG_WARN("Test warn message");
    LOG_ERROR("Test error message");
    assert(1); // Placeholder for actual test logic
    return 0;
}